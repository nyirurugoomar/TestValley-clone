/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ["dvd6ljcj7w3pj.cloudfront.net"], // <== Domain name
  },
};

export default nextConfig;
